name: TEST
on:
  workflow_dispatch:

  push:
    branches: [ "main" ]



jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3


    - name: Zip artifact for deployment
      run: |
        echo "path:" && pwd
        ls -hal
        cd src/data/
        echo "path:" && pwd
        echo "DONE."
        
